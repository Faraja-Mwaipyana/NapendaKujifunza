<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NapendaKujifunza</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            background-color: #ffffff;
            margin: 0;
            padding: 0;
            color: #333;
        }

        .container {
            width: 80%;
            margin: 0 auto;
            padding: 20px 0;
        }

        header {
            background-color: #0077b6;
            color: #ffffff;
            padding: 10px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            opacity: 0;
            animation: fadeIn 2s forwards;
        }

        header nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        header nav ul li {
            display: inline;
            margin-right: 20px;
        }

        header nav ul li a {
            color: #ffffff;
            text-decoration: none;
            font-weight: bold;
            font-size: 16px;
        }

        .hero {
            background-image: url('https://source.unsplash.com/1600x900/?education');
            background-size: cover;
            color: #ffffff;
            text-align: center;
            padding: 100px 0;
            opacity: 0;
            animation: fadeIn 2s forwards 1s;
        }

        .hero h2 {
            font-size: 36px;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 18px;
            margin-bottom: 40px;
        }

        .hero .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #ffffff;
            color: #0077b6;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .hero .btn:hover {
            background-color: #f0f0f0;
        }

        .featured-courses {
            padding: 40px 0;
        }

        .course-card {
            display: flex;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            overflow: hidden;
            opacity: 0;
            animation: fadeInUp 1s forwards;
        }

        .course-card:nth-child(2) {
            animation-delay: 0.2s;
        }

        .course-card:nth-child(3) {
            animation-delay: 0.4s;
        }

        .course-card:nth-child(4) {
            animation-delay: 0.6s;
        }

        .course-card img {
            width: 200px;
            height: 150px;
            object-fit: cover;
        }

        .course-card .course-info {
            padding: 20px;
        }

        .course-card h3 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .course-card p {
            font-size: 16px;
            color: #666;
            margin-bottom: 15px;
        }

        .course-card .btn {
            background-color: #0077b6;
            color: #ffffff;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .course-card .btn:hover {
            background-color: #005a8c;
        }

        footer {
            background-color: #0077b6;
            color: #ffffff;
            text-align: center;
            padding: 10px 0;
            position: fixed;
            bottom: 0;
            width: 100%;
            opacity: 0;
            animation: fadeIn 2s forwards;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>NapendaKujifunza</h1>
            <nav>
                <ul>
                    <li><a href="#">Home</a></li>
                    <li><a href="#courses">Courses</a></li>
                    <li><a href="#">Login</a></li>
                    <li><a href="#">Sign Up</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <section class="hero">
        <div class="container">
            <h2>Welcome to NapendaKujifunza</h2>
            <p>Expand your knowledge with our diverse selection of courses.</p>
            <a href="#courses" class="btn">Explore Courses</a>
        </div>
    </section>

    <section id="courses" class="featured-courses">
        <div class="container">
            <h2>Featured Courses</h2>
            <div class="course-card">
                <img src="https://source.unsplash.com/200x150/?web-development" alt="Course 1">
                <div class="course-info">
                    <h3>Introduction to Web Development</h3>
                    <p>A comprehensive guide to building websites from scratch.</p>
                    <a href="#" class="btn">Enroll Now</a>
                </div>
            </div>

            <div class="course-card">
                <img src="https://source.unsplash.com/200x150/?swahili" alt="Swahili Course">
                <div class="course-info">
                    <h3>Learn Swahili</h3>
                    <p>Master the basics of Swahili language and culture.</p>
                    <a href="#" class="btn">Enroll Now</a>
                </div>
            </div>

            <div class="course-card">
                <img src="https://source.unsplash.com/200x150/?virtual-assistant" alt="Virtual Assistant Course">
                <div class="course-info">
                    <h3>Virtual Assistant Training</h3>
                    <p>Develop skills to work as a virtual assistant.</p>
                    <a href="#" class="btn">Enroll Now</a>
                </div>
            </div>

            <div class="course-card">
                <img src="https://source.unsplash.com/200x150/?artificial-intelligence" alt="AI Course">
                <div class="course-info">
                    <h3>Artificial Intelligence Fundamentals</h3>
                    <p>Explore the basics of Artificial Intelligence.</p>
                    <a href="#" class="btn">Enroll Now</a>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <p>&copy; 2024 NapendaKujifunza. All rights reserved.</p>
        </div>
    </footer>

    <script>
        window.addEventListener('DOMContentLoaded', (event) => {
            const cards = document.querySelectorAll('.course-card');
            const options = {
                threshold: 0.1
            };

            const observer = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animation = 'fadeInUp 1s forwards';
                        observer.unobserve(entry.target);
                    }
                });
            }, options);

            cards.forEach(card => {
                observer.observe(card);
            });

            const links = document.querySelectorAll('header nav ul li a');
            links.forEach(link => {
                link.addEventListener('click', (e) => {
                    e.preventDefault();
                    const targetId = link.getAttribute('href').substring(1);
                    const targetSection = document.getElementById(targetId);
                    if (targetSection) {
                        window.scrollTo({
                            top: targetSection.offsetTop - 60,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>
